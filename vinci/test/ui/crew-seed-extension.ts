/**
 * Empty seed extension for crew view tests.
 * The real seeding is done via the seedCrew factory function in the test.
 */
export default function (_pi: any) {
	// Seed function is applied via factory in test
}
